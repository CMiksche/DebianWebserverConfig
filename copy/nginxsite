server {
        listen 80;
        listen [::]:80;

        server_name yourdomain.com;

        root /var/www/html/yourdomain.com;
        index index.html;

        location / {
                try_files $uri $uri/ =404;
        }
}
